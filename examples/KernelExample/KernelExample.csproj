<Project Sdk="Microsoft.NET.Sdk">

  <Sdk Name="Cosmos.Sdk" Version="3.0.0" />

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RuntimeIdentifier Condition="'$(RuntimeIdentifier)' == ''">linux-x64</RuntimeIdentifier>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <IlcSystemModule>System.Private.CoreLib</IlcSystemModule>

    <!-- Cosmos.Sdk is not compatible with Global Package managment -->
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <InvariantGlobalization>true</InvariantGlobalization>
    <SelfContained>true</SelfContained>
    <PublishTrimmed>true</PublishTrimmed>
  </PropertyGroup>

  <!-- Add any folder(s) where *.c files exist -->
  <ItemGroup>
    <GCCProject Include="./src/C/" />
  </ItemGroup>

  <!-- PInvokes to be resolved. See https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/interop -->
  <ItemGroup>
    <DirectPInvoke Include="test" />
    <IlcArg Include="--verbose" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Cosmos.Kernel" Version="3.0.0" />
    <!-- Architecture-specific native package selected based on RuntimeIdentifier -->
    <PackageReference Include="Cosmos.Kernel.Native.x64" Version="3.0.0" Condition="'$(RuntimeIdentifier)' == 'linux-x64' OR '$(RuntimeIdentifier)' == 'win-x64' OR '$(RuntimeIdentifier)' == 'osx-x64'" />
    <PackageReference Include="Cosmos.Kernel.Native.ARM64" Version="3.0.0" Condition="'$(RuntimeIdentifier)' == 'linux-arm64' OR '$(RuntimeIdentifier)' == 'win-arm64' OR '$(RuntimeIdentifier)' == 'osx-arm64'" />
    <!-- Always use x64 ILC tools for cross-compilation, but include ARM64 runtime libraries when needed -->
    <PackageReference Include="runtime.linux-x64.Microsoft.DotNet.ILCompiler" Version="9.0.8" />
    <PackageReference Include="runtime.linux-arm64.Microsoft.DotNet.ILCompiler" Version="9.0.8" Condition="'$(RuntimeIdentifier)' == 'linux-arm64'" />
  </ItemGroup>
</Project>
