<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Local development packages (only works within the repo) -->
    <!-- If you copied this example outside the repo, delete this file -->
    <add key="local-packages" value="../../artifacts/package/release" />
    <!-- Falls back to global nuget.org if local packages don't exist -->
  </packageSources>
</configuration>