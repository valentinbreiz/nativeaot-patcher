<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../Cosmos.TestRunner.Protocol/Cosmos.TestRunner.Protocol.csproj" />
    <!-- Reuse the QEMU launcher and tool resolver from cosmos run so tests and CLI agree on argv. -->
    <Compile Include="../../src/Cosmos.Tools/Launcher/*.cs" />
    <Compile Include="../../src/Cosmos.Tools/Platform/*.cs" />
  </ItemGroup>

</Project>
