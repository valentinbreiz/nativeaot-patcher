<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!--pack as nuget-->
    <IsPackable>true</IsPackable>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <!--pack as nuget-->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <!-- Add any folder(s) where *.asm files exist -->
  <ItemGroup>
    <AsmSearchPath Include="./src/Asm/" />
    <!-- You can also do: <AsmFiles Include="/path/to/file.asm" /> -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\\Cosmos.Kernel.Core\\Cosmos.Kernel.Core.csproj" />
  </ItemGroup>
  <ItemGroup>
  <Compile Include="$(AotCommonPath)Internal\Runtime\TypeManagerHandle.cs">
    <Link>Internal\Runtime\TypeManagerHandle.cs</Link>
  </Compile>
    <Compile Include="$(ToolCommonPath)Internal\Runtime\ModuleHeaders.cs">
      <Link>Internal\Runtime\ModuleHeaders.cs</Link>
    </Compile>
  </ItemGroup>

</Project>
